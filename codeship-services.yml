db:
  image: postgres:9.5
app:
  build:
    image: rheinwein/notes-app 
    dockerfile: Dockerfile.test
  cached: true
  links:
    - db
deploy:
  build:
    dockerfile: Dockerfile.deploy
  cached: true

